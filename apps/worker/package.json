{
    "name": "@kairos/worker",
    "version": "0.0.1",
    "private": true,
    "description": "Indexer + billing scheduler + webhook dispatcher. Implemented from Phase 1 onwards.",
    "license": "MIT",
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run --passWithNoTests",
        "dev": "tsx src/index.ts"
    },
    "dependencies": {
        "@kairos/core": "workspace:*",
        "@solana-program/token": "^0.13.0",
        "@solana/kit": "^6.4.0",
        "@solana/kit-plugin-rpc": "^0.11.1",
        "@solana/kit-plugin-signer": "^0.10.0",
        "@solana/subscriptions": "0.3.0",
        "drizzle-orm": "^0.45.2"
    },
    "devDependencies": {
        "@types/node": "^20.17.0",
        "tsx": "^4.19.0",
        "typescript": "^5.6.0",
        "vitest": "^3.0.0"
    }
}
